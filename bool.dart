void main() {
  // Q16 Declare a boolean variable isStudent = true and print it.
  bool isStudent = true;
  print(isStudent);

  // Q17 Create a boolean variable isLoggedIn = false and print it.
  bool isLoggedIn = false;
  print(isLoggedIn);

  // Q18 Declare a boolean variable hasInternet = true. Print its value.
  bool hasInternet = true;
  print(hasInternet);

  // Q19 Store whether today is Sunday in a boolean variable.
  bool isSunday = false;
  print(isSunday);

  // Q20 Create a boolean variable representing whether you have completed today's assignment.
  bool assignmentCompleted = true;
  print(assignmentCompleted);
}
