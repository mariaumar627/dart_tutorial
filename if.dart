void main() {
  //If statement practice questions
  //Q1: eligible for cnic if age is 18 or above
  int age = 20;
  if (age >= 18) {
    print('eligible for CNIC');
  }

  // Q2 print positive num if it is greater than zero
  int num = 8;
  if (num >= 0) {
    print('positive number');
  }

  //Q3 print hot weather if temperature is greater than 35
  int temperature = 50;
  if (temperature >= 35) {
    print('Hot Weather');
  }

  //Q4 print strong password if length if length is 8 or more
  String password = "mypassword";
  if (password.length >= 8) {
    print('strong password');
  }
  //Q5 print passed if marks  are 40 or above
  int marks = 60;
  if (marks >= 40) ;
  print('Passed');
}
